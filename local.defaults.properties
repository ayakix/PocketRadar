# Defaults injected by secrets-gradle-plugin when local.properties is missing a key.
# Real values belong in local.properties (gitignored). This placeholder lets the
# build succeed even before MAPS_API_KEY is set, although the map will not load.
MAPS_API_KEY=YOUR_MAPS_API_KEY_HERE
